# For the FastAPI server
fastapi
uvicorn

# For the Pi client
websockets

# For the dashboard
# (no Python requirements; runs in browser)
fastapi>=0.110,<1.0
uvicorn[standard]>=0.29,<1.0
websockets>=12.0,<13.0
